{
  "name": "mbabazi-closet-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm install --prefix backend && npm install --prefix frontend --include=dev && NODE_OPTIONS='--max-old-space-size=2048' npm run build --prefix frontend",
    "start": "node backend/server.js"
  },
  "dependencies": {
    "mongodb": "^7.1.0"
  }
}
